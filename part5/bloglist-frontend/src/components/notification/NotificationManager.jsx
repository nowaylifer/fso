import React from 'react';

let error;
let success;

const NotificationManager = (p) => {
  const [notifications, setNotifications] = React.useState([]);

  return;
};

export default NotificationManager;
